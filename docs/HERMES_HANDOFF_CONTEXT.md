# Hermes handoff context

Primary handoff source: `C:\Users\manaz\.hermes\knowledge\ox-alpha-master-handoff-bundle.md`.

The bundle is broader than LeadFinder. For this project, the relevant operating rules are:

- Evidence hierarchy: shipped GitHub default branch > Maz Works Knowledge Vault > local worktree > chat notes. Label unknowns instead of guessing.
- Maz Works is a one-builder UK agency with a bounded £150 founding implementation offer; the public site must turn proof into a qualified conversation without agency theatre.
- Keep JobFilterV1, MazOS, `mazos-site` and LeadFinder as separate products. Do not use JobFilter procurement semantics or runtime dependencies in LeadFinder.
- Prefer the smallest evidence-producing vertical slice. Validate demand and false positives before adding infrastructure.
- Preserve human control, public-safe data handling, privacy boundaries, honest limitations and reversible changes.
- For portfolio/site changes, lead with real proof, named outcomes, a clear bounded next step and measurable contact intent.

When a later prompt batch changes LeadFinder or Maz Works commercial direction, append a dated decision to `IMPLEMENTATION_PLAN.md` and add a regression/acceptance check before implementation.
